import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Maifadi Financial Solutions | Effective Financial Solutions",
  description: "Accounting, Tax, and Business Advisory services for SMMEs and Government Entities in South Africa.",
  keywords: ["Accounting Pretoria", "Tax Services Johannesburg", "Payroll Solutions South Africa", "Maifadi Financial"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
